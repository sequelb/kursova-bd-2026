# РОЗДІЛ 2. РОЗРОБКА МОДЕЛЕЙ ДАНИХ

## 2.1. Концептуальне проектування

На етапі концептуального проектування було визначено основні сутності предметної області та зв'язки між ними. Результатом є діаграма «сутність — зв'язок» (ER-діаграма), що відображає структуру даних на рівні понять, без прив'язки до конкретної СУБД.

Предметна область «навчальна онлайн-платформа» охоплює такі взаємопов'язані сутності:

- *Користувач* (User) — центральна сутність, що описує будь-якого зареєстрованого учасника системи. Має атрибути імені, прізвища, електронної пошти, ролі (студент, викладач або адміністратор) та статусу облікового запису (активний або призупинений);
- *Профіль викладача* (TeacherProfile) — розширення користувача з роллю «викладач». Містить біографію та поточний баланс нарахованих коштів. Зв'язок 1:1 із користувачем;
- *Курс* (Course) — навчальний курс, створений викладачем. Має назву, опис, ціну, рівень складності, статус публікації та дату створення. Зв'язок N:1 із профілем викладача (автор);
- *Категорія* (Category) — тематична класифікація. Зв'язок M:N із курсами через проміжну таблицю;
- *Урок* (Lesson) — окремий елемент курсу з назвою, текстовим вмістом та порядковим номером. Зв'язок N:1 із курсом;
- *Запис на курс* (Enrollment) — реєстрація студента на курс. Фіксує дату та відсоток прогресу. Зв'язок N:1 із користувачем (студентом) та N:1 із курсом;
- *Прогрес уроку* (LessonProgress) — факт завершення конкретного уроку в межах конкретного запису. Зв'язок N:1 із записом та N:1 із уроком. Має композитний первинний ключ;
- *Відгук* (Review) — оцінка та коментар до курсу. Первинним ключем є ідентифікатор запису на курс, що структурно гарантує обмеження «один відгук на один запис». Зв'язок 1:1 із записом;
- *Оплата* (Payment) — фінансова транзакція при записі на курс. Зв'язок N:1 із користувачем та N:1 із курсом;
- *Виплата* (Payout) — запит викладача на виведення коштів. Зв'язок N:1 із профілем викладача.

Порівняно з початковою ER-діаграмою, поданою у проміжному звіті, фінальна версія має такі відмінності:

а) сутність «Студент» (Student) вилучена — вона не мала власних атрибутів і була надмірною. Студент — це користувач із роллю «Student»;
б) сутність «Автор» (Author) перейменована на «Профіль викладача» (TeacherProfile) для точнішого відображення призначення;
в) додано сутність «Категорія» (Category) як окрему таблицю зі зв'язком M:N через проміжну таблицю «course_categories». В початковій діаграмі категорія була атрибутом курсу;
г) додано сутність «Прогрес уроку» (LessonProgress) для відстеження завершення окремих уроків;
ґ) додано атрибут «Статус» (Status) до сутності «Користувач» для підтримки механізму призупинення облікових записів.

Фінальну ER-діаграму подано на рис. 2.1.

[Рис. 2.1. Діаграма «сутність — зв'язок» інформаційної системи]

## 2.2. Вимоги до системи накопичення даних

### Оцінка обсягів даних

Система розрахована на роботу з обсягом даних навчальної онлайн-платформи середнього розміру. Для тестування та демонстрації використовується набір із понад 3 600 реальних курсів (з відкритого набору даних Udemy Courses на платформі Kaggle), доповнений згенерованими записами. Загальний обсяг тестових даних перевищує 50 000 записів:

- користувачів (викладачі + студенти): ~2 500;
- курсів: ~3 600;
- уроків: ~20 000;
- записів на курси: ~18 000;
- записів прогресу уроків: ~10 000;
- відгуків: ~14 000;
- оплат: ~18 000;
- виплат: ~1 500.

### Частота додавання

Транзакційні записи (оплати, записи на курси, прогрес) створюються при кожній дії користувача. Довідкові дані (категорії) змінюються рідко. Курси та уроки створюються та редагуються викладачами на регулярній основі.

### Очікувані запити та вибірки

Основні типи запитів, що формуються інтерфейсом:

- *Каталог курсів* — вибірка опублікованих курсів із фільтрацією за категоріями, ціною, рівнем складності та рейтингом, повнотекстовим пошуком за назвою, сортуванням за різними критеріями та пагінацією;
- *Аналітика курсу* — агрегація кількості записів, загального доходу, середньої оцінки та відсотку завершення по конкретному курсу, а також часовий ряд реєстрацій за обраний період;
- *Фінансова панель* — агрегація оплат і виплат за довільний часовий діапазон з обчисленням підсумкових показників;
- *Рекомендації* — складний аналітичний запит із тристороннім самооб'єднанням таблиці записів для виявлення шаблонів спільних реєстрацій.

### Обмеження доступу

Доступ до даних обмежується на рівні серверного API відповідно до ролі автентифікованого користувача:

- студент має доступ лише до власних записів, прогресу та відгуків, а також до каталогу опублікованих курсів;
- викладач має доступ лише до власних курсів, уроків, аналітики та виплат;
- адміністратор має доступ до фінансових даних усіх користувачів та управління їх статусом.

Неавтентифіковані запити отримують відповідь із кодом 401 (Unauthorized), а запити з недостатніми правами — 403 (Forbidden).

## 2.3. Логічне проектування бази даних

Схема бази даних складається з 11 таблиць. Усі назви таблиць та стовпців використовують формат snake_case, що забезпечується бібліотекою EFCore.NamingConventions. Нижче подано опис кожної таблиці.

Таблиця *users* (табл. 2.1) зберігає облікові записи усіх користувачів системи.

Таблиця 2.1.
Структура таблиці users

| Стовпець | Тип | Обмеження |
|---|---|---|
| id | integer | PK, автоінкремент |
| first_name | varchar(100) | NOT NULL |
| last_name | varchar(100) | NOT NULL |
| email | varchar(255) | NOT NULL, UNIQUE |
| password_hash | text | NOT NULL |
| role | varchar(20) | NOT NULL, CHECK (role IN ('Admin', 'Teacher', 'Student')) |
| status | varchar(20) | NOT NULL, DEFAULT 'Active', CHECK (status IN ('Active', 'Suspended')) |

Таблиця *teacher_profiles* (табл. 2.2) є розширенням для користувачів із роллю «Викладач».

Таблиця 2.2.
Структура таблиці teacher_profiles

| Стовпець | Тип | Обмеження |
|---|---|---|
| user_id | integer | PK, FK → users(id), ON DELETE CASCADE |
| bio | varchar(2000) | |
| balance | numeric(10,2) | DEFAULT 0 |

Таблиця *categories* (табл. 2.3) є довідником тематичних категорій курсів.

Таблиця 2.3.
Структура таблиці categories

| Стовпець | Тип | Обмеження |
|---|---|---|
| id | integer | PK, автоінкремент |
| name | varchar(60) | NOT NULL, UNIQUE |

Таблиця *courses* (табл. 2.4) зберігає навчальні курси, створені викладачами. Зовнішній ключ author_id посилається на teacher_profiles(user_id), а не на users(id), що структурно гарантує: автором курсу може бути лише користувач із профілем викладача.

Таблиця 2.4.
Структура таблиці courses

| Стовпець | Тип | Обмеження |
|---|---|---|
| id | integer | PK, автоінкремент |
| author_id | integer | FK → teacher_profiles(user_id), ON DELETE RESTRICT |
| title | varchar(200) | NOT NULL |
| description | text | NOT NULL |
| price | numeric(10,2) | CHECK (price >= 0) |
| level | varchar(20) | NOT NULL, CHECK (level IN ('Beginner', 'Intermediate', 'Advanced')) |
| status | varchar(20) | NOT NULL, CHECK (status IN ('Draft', 'Published')) |
| created_at | timestamp | DEFAULT now() |

Таблиця *course_categories* (табл. 2.5) є проміжною таблицею для зв'язку M:N між курсами та категоріями.

Таблиця 2.5.
Структура таблиці course_categories

| Стовпець | Тип | Обмеження |
|---|---|---|
| courses_id | integer | PK (частина), FK → courses(id) |
| categories_id | integer | PK (частина), FK → categories(id) |

Таблиця *lessons* (табл. 2.6) зберігає уроки в межах курсу.

Таблиця 2.6.
Структура таблиці lessons

| Стовпець | Тип | Обмеження |
|---|---|---|
| id | integer | PK, автоінкремент |
| course_id | integer | FK → courses(id), ON DELETE CASCADE |
| order_number | integer | UNIQUE разом із course_id |
| title | varchar(200) | NOT NULL |
| content | text | NOT NULL |

Таблиця *enrollments* (табл. 2.7) зберігає записи студентів на курси. Комбінація (student_id, course_id) має обмеження UNIQUE — студент може записатись на курс лише один раз.

Таблиця 2.7.
Структура таблиці enrollments

| Стовпець | Тип | Обмеження |
|---|---|---|
| id | integer | PK, автоінкремент |
| student_id | integer | FK → users(id), ON DELETE RESTRICT |
| course_id | integer | FK → courses(id), ON DELETE RESTRICT |
| enrolled_at | timestamp | DEFAULT now() |
| progress | integer | DEFAULT 0, CHECK (progress BETWEEN 0 AND 100) |

Таблиця *lesson_progress* (табл. 2.8) відстежує завершення окремих уроків. Композитний первинний ключ (enrollment_id, lesson_id) гарантує, що кожен урок може бути позначений як завершений лише один раз у межах одного запису.

Таблиця 2.8.
Структура таблиці lesson_progress

| Стовпець | Тип | Обмеження |
|---|---|---|
| enrollment_id | integer | PK (частина), FK → enrollments(id), ON DELETE CASCADE |
| lesson_id | integer | PK (частина), FK → lessons(id), ON DELETE RESTRICT |
| completed_at | timestamp | DEFAULT now() |

Таблиця *reviews* (табл. 2.9) зберігає відгуки студентів на курси. Первинний ключ enrollment_id одночасно є зовнішнім ключем до таблиці enrollments, що забезпечує два обмеження: один відгук на один запис та неможливість залишити відгук без попереднього запису на курс.

Таблиця 2.9.
Структура таблиці reviews

| Стовпець | Тип | Обмеження |
|---|---|---|
| enrollment_id | integer | PK, FK → enrollments(id), ON DELETE CASCADE |
| grade | integer | CHECK (grade BETWEEN 1 AND 5) |
| comment | varchar(2000) | |
| created_at | timestamp | DEFAULT now() |

Таблиця *payments* (табл. 2.10) зберігає фінансові транзакції при записі на курс.

Таблиця 2.10.
Структура таблиці payments

| Стовпець | Тип | Обмеження |
|---|---|---|
| id | integer | PK, автоінкремент |
| student_id | integer | FK → users(id), ON DELETE RESTRICT |
| course_id | integer | FK → courses(id), ON DELETE RESTRICT |
| amount | numeric(10,2) | CHECK (amount >= 0) |
| status | varchar(20) | NOT NULL, CHECK (status IN ('Completed', 'Refunded')) |
| created_at | timestamp | DEFAULT now() |

Таблиця *payouts* (табл. 2.11) зберігає запити викладачів на виплату коштів.

Таблиця 2.11.
Структура таблиці payouts

| Стовпець | Тип | Обмеження |
|---|---|---|
| id | integer | PK, автоінкремент |
| teacher_id | integer | FK → teacher_profiles(user_id), ON DELETE RESTRICT |
| amount | numeric(10,2) | CHECK (amount >= 0) |
| status | varchar(20) | NOT NULL, CHECK (status IN ('Pending', 'Approved', 'Rejected', 'Paid')) |
| requested_at | timestamp | DEFAULT now() |

### Нормалізація

Спроектована схема відповідає третій нормальній формі (3НФ):

- *1НФ* — кожен стовпець містить атомарні значення; зв'язок M:N між курсами та категоріями реалізовано через окрему проміжну таблицю course_categories, а не через масив значень;
- *2НФ* — усі неключові атрибути повністю функціонально залежать від первинного ключа. У таблицях із композитним ключем (lesson_progress, course_categories) немає часткових залежностей;
- *3НФ* — відсутні транзитивні залежності. Зокрема, поле balance у таблиці teacher_profiles є обчислюваним значенням, проте воно підтримується тригерами для оптимізації продуктивності запитів і завжди узгоджене з базовими таблицями payments та payouts.

### Індекси

Окрім неявних індексів, створених для первинних ключів та обмежень UNIQUE, визначено 5 явних індексів для оптимізації типових запитів (табл. 2.12).

Таблиця 2.12.
Явні індекси бази даних

| Індекс | Таблиця, стовпець | Обґрунтування |
|---|---|---|
| idx_courses_author_id | courses(author_id) | Вибірка курсів конкретного викладача |
| idx_enrollments_student_id | enrollments(student_id) | Вибірка записів конкретного студента |
| idx_enrollments_course_id | enrollments(course_id) | Аналітика курсу (підрахунок студентів) |
| idx_payments_course_id | payments(course_id) | Аналітика курсу (обчислення доходу) |
| idx_payouts_teacher_id | payouts(teacher_id) | Перегляд виплат конкретного викладача |

Кожен індекс відповідає стовпцю зовнішнього ключа, за яким часто виконуються запити фільтрації через інтерфейс користувача.

## 2.4. Реалізація процедур бізнес-логіки

На рівні бази даних реалізовано три тригери та дві допоміжні функції мовою PL/pgSQL, що забезпечують автоматичне підтримання цілісності обчислюваних значень.

### 2.4.1. Тригер перерахунку прогресу навчання

Тригер *trg_lesson_progress_recompute* спрацьовує після вставки або видалення запису в таблиці lesson_progress (подія AFTER INSERT OR DELETE) та викликає функцію *recompute_enrollment_progress*. Алгоритм: визначається загальна кількість уроків курсу та кількість завершених уроків для конкретного запису, після чого прогрес обчислюється як відсоток завершених уроків від загальної кількості (ціле число від 0 до 100).

### 2.4.2. Тригер перерахунку балансу при оплаті

Тригер *trg_payments_balance* спрацьовує після вставки нового запису або зміни стовпця status у таблиці payments (подія AFTER INSERT OR UPDATE OF status) та викликає функцію *trg_payments_recompute_balance*. Функція знаходить автора відповідного курсу та делегує обчислення спільній допоміжній функції *recompute_teacher_balance*.

### 2.4.3. Тригер перерахунку балансу при виплаті

Тригер *trg_payouts_balance* спрацьовує після вставки або зміни стовпця status у таблиці payouts (подія AFTER INSERT OR UPDATE OF status) та викликає функцію *trg_payouts_recompute_balance*, яка делегує обчислення тій самій допоміжній функції *recompute_teacher_balance*.

### 2.4.4. Допоміжна функція recompute_teacher_balance

Обидва фінансових тригери делегують обчислення спільній функції, що приймає ідентифікатор викладача та перераховує його баланс за формулою (2.1):

баланс = Σ(завершені оплати за курси викладача) − Σ(невідхилені виплати викладачу)     (2.1)

Функція повністю перераховує баланс із нуля при кожному виклику, що гарантує узгодженість навіть після ручних корекцій в базі даних.

### 2.4.5. Збережена функція get_recommendations

Алгоритм рекомендацій реалізовано як збережену функцію PostgreSQL, що приймає ідентифікатор студента та максимальну кількість результатів. Детальний опис алгоритму подано у підрозділі 2.5.

## 2.5. Алгоритм рекомендацій на основі асоціативних правил

### 2.5.1. Теоретичні основи

Для реалізації додаткової функції підбору рекомендованих курсів обрано метод *видобування асоціативних правил* (association rule mining), також відомий як *аналіз ринкового кошика* (market basket analysis). Цей метод аналізу даних дозволяє виявляти закономірності в поведінці користувачів: які курси часто обираються разом.

Асоціативне правило має форму {A} → {B}, що означає: «студенти, які записались на курс A, також схильні записуватись на курс B». Кожне правило оцінюється за трьома стандартними метриками.

*Підтримка* (support) — частка студентів, записаних одночасно на обидва курси, серед усіх студентів:

support(A → B) = |студенти, записані на A та B| / |усі студенти|     (2.2)

Вища підтримка означає, що шаблон є більш поширеним і, відповідно, надійнішим.

*Впевненість* (confidence) — умовна ймовірність того, що студент, записаний на курс A, також записаний на курс B:

confidence(A → B) = |студенти, записані на A та B| / |студенти, записані на A|     (2.3)

Впевненість відображає передбачувальну силу правила.

*Підйом* (lift) — відношення впевненості до безумовної ймовірності запису на курс B:

lift(A → B) = confidence(A → B) / P(B), де P(B) = |студенти, записані на B| / |усі студенти|     (2.4)

Підйом показує, наскільки асоціація є сильнішою за випадковий збіг:

- lift > 1 — зв'язок сильніший за випадковий, реальна закономірність;
- lift = 1 — зв'язку немає, курси обираються незалежно;
- lift < 1 — від'ємна асоціація, курси обираються рідше, ніж очікувалось.

### 2.5.2. Формула оцінки

Для кожного курсу-кандидата обчислюється композитна оцінка, що поєднує п'ять сигналів:

score = Σ(confidence × lift × completion_weight) × rating_multiplier × category_multiplier     (2.5)

*Сигнал «Асоціативні правила»* (confidence × lift). Для кожного курсу, на який записаний поточний студент, алгоритм знаходить шаблони спільних записів з іншими студентами. Множення впевненості та підйому забезпечує пріоритет правил, які є одночасно передбачувальними (висока впевненість) та нетривіальними (високий підйом).

*Сигнал «Вага завершення»* (completion_weight). Не всі однокурсники є рівноцінними сигналами. Студент, який завершив спільний курс, є сильнішим індикатором, ніж той, хто записався, але не розпочав навчання:

completion_weight = 0.5 + 0.5 × (прогрес_однокурсника / 100)     (2.6)

Мінімальна вага 0.5 означає, що сам факт запису є значущим сигналом — студент свідомо обрав та оплатив курс. Завершення посилює цей сигнал, але не є обов'язковою умовою.

*Сигнал «Якість рейтингу»* (rating_multiplier). Курси з вищим середнім рейтингом отримують перевагу:

rating_multiplier = COALESCE(середній_рейтинг, 3.0) / 5.0     (2.7)

Курси без відгуків отримують нейтральне значення 3.0 з 5.0, щоб не штрафувати нові курси.

*Сигнал «Афінність категорій»* (category_multiplier). Якщо рекомендований курс має спільні категорії з курсами, на які вже записаний студент, він отримує невелику надбавку:

category_multiplier = 1.0 + 0.2 × кількість_спільних_категорій     (2.8)

Коефіцієнт 0.2 обрано навмисно низьким — це сигнал-розв'язувач, а не основний фактор.

*Сигнал «Обробка холодного старту».* Для студентів із нульовою кількістю записів асоціативні правила не можуть бути застосовані. У такому випадку алгоритм повертає найпопулярніші курси за кількістю записів. Щойно студент записується на перший курс, рекомендації стають персоналізованими.

### 2.5.3. Реалізація

Алгоритм реалізовано як збережену функцію PostgreSQL *get_recommendations(target_student_id, max_results)*, що повертає набір записів (course_id, score, confidence, lift, reason). Функція використовує 9 послідовних загальних табличних виразів (CTE — Common Table Expression):

а) *my_courses* — курси поточного студента;
б) *my_categories* — категорії цих курсів;
в) *course_enrollment_counts* — кількість записів на кожен курс (знаменники для confidence та lift);
г) *peer_enrollments* — тристороннє самооб'єднання таблиці enrollments: курси студента → однокурсники → інші курси однокурсників;
ґ) *rule_metrics* — confidence, lift та зважена кількість однокурсників для кожного правила;
д) *best_rule* — найсильніше правило для кожного рекомендованого курсу (для тексту пояснення);
е) *avg_ratings* — середній рейтинг кожного курсу;
є) *cat_overlap* — кількість спільних категорій;
ж) *scored* — фінальна композитна оцінка.

Кожна рекомендація супроводжується текстовим поясненням, наприклад: «47% студентів, які проходили "React for Beginners", також обрали цей курс (1.8× вище середнього)».
